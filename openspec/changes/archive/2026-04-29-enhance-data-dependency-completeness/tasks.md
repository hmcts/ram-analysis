## 1. Ship the manual-flow keyword scan

- [x] 1.1 Create `.claude/lib/create-data-dependency-architecture/scripts/find-manual-flows.sh` as a POSIX shell script that takes `<input-folder>` as its single argument, validates that `<input-folder>/output/extracted-text/` exists, and exits non-zero with a clear message otherwise.
- [x] 1.2 Embed the canonical phrase list at the top of the script as a single newline-separated heredoc / array — the 23 phrases listed in the spec (`copies from`, `copy and paste`, `copy/paste`, `manually enter`, `manually entered`, `manually copied`, `rekey`, `re-keys`, `rekeyed`, `transcribe`, `transcribed`, `look up in`, `looks up in`, `look up from`, `is entered by`, `data is entered`, `obtained from`, `populated from`, `populated by`, `sourced from`, `no integration`, `no automated integration`, `out-of-band`).
- [x] 1.3 Implement the scan: iterate over `<input-folder>/output/extracted-text/*.txt`, run a case-insensitive fixed-string `grep -nFi` for each phrase, and emit lines of the form `<filename>:<line>: <matching line>` to `<input-folder>/output/manual-flow-candidates.txt`. Sort the output (by filename, then line number) so the file is byte-stable across runs.
- [x] 1.4 Make the script idempotent: rewrite (don't append to) `manual-flow-candidates.txt` on every run; do not write any temp files outside `<input-folder>/output/`.
- [x] 1.5 `chmod +x` the script and verify it runs from any working directory (use absolute or `$1`-relative paths internally — never assume `pwd`).
- [x] 1.6 Smoke-test the script against `docs/` (or any existing `extracted-text/` folder produced by a prior run) and confirm `manual-flow-candidates.txt` is produced and sorted.

## 2. Update SKILL.md to add the new Phase 2 sub-phases and gating

- [x] 2.1 In `.claude/lib/create-data-dependency-architecture/SKILL.md`, expand Phase 2a so it explicitly produces three artefacts before classification: `output/system-enumeration.md` (table of every named system with canonical name / aliases / classification / status / citation), `output/system-aliases.json` (canonical-to-aliases + aliases-to-canonical maps), and `output/manual-flow-candidates.txt` (generated by `scripts/find-manual-flows.sh`).
- [x] 2.2 Add a new step instructing the model to run `scripts/find-manual-flows.sh <input-folder>` immediately after Phase 1 distillation completes; document that the script's output is the discovery floor that Phase 2a/2d must reconcile.
- [x] 2.3 Replace the prose Phase 2d cross-check with a directive to write `<input-folder>/output/phase-2d-checks.md` containing the seven fixed checklist items from `specs/data-dependency-discovery/spec.md` (Requirement: Phase 2d Cross-Check Artefact); each item must be marked ✅ with a one-line justification or ❌ with the resolution action.
- [x] 2.4 Add an explicit gate before Phase 3: "Do not author `data-dependencies.md` until `system-enumeration.md`, `system-aliases.json`, `manual-flow-candidates.txt` and `phase-2d-checks.md` all exist and `phase-2d-checks.md` has every item marked ✅."
- [x] 2.5 Update Phase 2b's classification rules to enumerate the closed `Status` vocabulary (the five permitted strings) and to require the `Manual copy` value for any flow where a human regularly transcribes from a named upstream system.
- [x] 2.6 Update the "Anti-patterns to avoid" section to add: "Don't drop a dependency just because the source documents say 'no integration' — if there is a named upstream system whose data is moved manually, it is a `Manual copy` inbound dependency."
- [x] 2.7 Add the new artefact list to the "What goes where — tooling vs data" section so the four new files appear under `<input-folder>/output/`.

## 3. Update OUTPUT-STRUCTURE.md and STYLE-GUIDE.md to enforce the closed Status vocabulary

- [x] 3.1 In `.claude/lib/create-data-dependency-architecture/references/OUTPUT-STRUCTURE.md`, replace the existing `Status` description with the closed list of five values, and add a sentence stating the values are matched verbatim.
- [x] 3.2 In the same file, update the example `Attribute / Detail` tables for inbound (8 rows) and outbound (9 rows) so the `Status` cell uses one of the five permitted values.
- [x] 3.3 Add a new sub-section after Section 3 (`## At a Glance`) titled "Manual-copy dependencies are catalogued" that summarises Requirement "Manual-Copy Dependencies Must Be Catalogued" in 2-3 sentences for the author.
- [x] 3.4 In `.claude/lib/create-data-dependency-architecture/references/STYLE-GUIDE.md`, update the example At a Glance row's `Mechanism` cell example to show "Manual copy by RSU" (already present) and add an explicit note that `Status` values come from the closed vocabulary defined in `OUTPUT-STRUCTURE.md`.

## 4. Update LESSONS-LEARNED.md with the new lessons

- [x] 4.1 Add a new lesson "**18. Manual-copy dependencies are first-class**" under the "Document content" heading explaining that 'no integration' is not 'no dependency' when there is a named upstream system, and pointing at `Status` = `Manual copy`.
- [x] 4.2 Add a new lesson "**19. Discovery has a floor, not a ceiling — the keyword scan is the floor**" explaining what `find-manual-flows.sh` is for, why it exists, and that the model's job is to interpret each candidate, not to find them all from prose.
- [x] 4.3 Add a new lesson "**20. Phase 2 produces auditable artefacts**" explaining the four new files (`system-enumeration.md`, `system-aliases.json`, `manual-flow-candidates.txt`, `phase-2d-checks.md`), why each one is required, and why authoring is gated on them.

## 5. Update the markdown template

- [x] 5.1 In `.claude/lib/create-data-dependency-architecture/templates/data-dependencies.template.md`, update the `Status` row in the inbound `Attribute | Detail` example to read `| **Status** | Manual copy |` (replacing the placeholder `{{STATUS_1_FULL}}` example with one of the closed values).
- [x] 5.2 Same change to the outbound example: replace the placeholder with `| **Status** | Implemented (automated) |`.
- [x] 5.3 Add a short HTML comment immediately above each `Status` row in the template listing the five permitted values, so authors looking at the template see the closed enum without needing to open `OUTPUT-STRUCTURE.md`.

## 6. Update the command file (no behavioural change, just doc accuracy)

- [x] 6.1 In `.claude/commands/create-data-dependency-architecture.md`, add `scripts/find-manual-flows.sh` to the bullet list of supporting assets, with a one-line description ("manual-flow keyword scan; produces `output/manual-flow-candidates.txt` as the Phase 2 discovery floor").

## 7. Smoke-test against the JI documents

- [x] 7.1 Run the updated skill end-to-end against the existing JI source folder used in prior runs. Capture `output/system-enumeration.md`, `output/system-aliases.json`, `output/manual-flow-candidates.txt`, `output/phase-2d-checks.md` for review.
- [x] 7.2 Diff the resulting `data-dependencies.md` row count and canonical-name set against the previous run's `data-dependencies.md`. Confirm any added rows are real manual-copy or NFR-stated dependencies that the previous run missed; confirm no real dependencies were dropped.
- [x] 7.3 Re-run the skill a second time without changing the source documents. Confirm `data-dependencies.md` has the same canonical-name set, the same Status values, the same row count and the same alias collapsing as the first re-run (Requirement: Same Inputs Produce the Same Catalogue).
- [x] 7.4 Confirm the rendered PDF still builds (`scripts/build-pdf.sh <input-folder>/output/data-dependencies.md`) and `pdfinfo` reports a non-zero page count and a sensible file size.
- [x] 7.5 Confirm no files outside `<input-folder>/output/` were created or modified by the skill run (`git status` in the running repo shows only the intentional skill-file edits from tasks 1–6).
